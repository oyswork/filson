# Filson -  язык для осуществления выборки из массивов данных.
Предложения с другими именами приветствуются. Текущее было рождено так - **Fil**tering j**son**s.

## Простые типы данных

 - `integer` - целое число со знаком вплоть до 64 бит. Формат - числа с опциональным разделителем `_` между цифрами и опциональными `+/-` спереди. 
	 - 1
	- +1 
	- -1 
	- 1_2

   
 - `float` - число с плавающей точкой и знаком, вплоть до 64 бит.
	Формат - допустимы опциональные разделители между цифрами `_` и опциональная
   часть с экспонентой, которая обозначается `e/E` и в которой тоже
   допускаются знаки и разделители. Целочисленная часть может
   отсутствовать.  
	 - 1.2, 
	 - +1.2, 
	 - -1.2,
	 -  .1, 
	 - 1_2.34, 
	 - 12.3_4, 
	 - 1.0e1,
	 - 1.0E1
	 - 1.0e+1
	 - 1.0e-1
	 - 1.0e1_0

   

 - `boolean`
	 - true
	 - false
   
   
 - `null` 
	 - null

   

 - `string` - любая валидная utf-8 строка в кавычках.
	 - `"\"this is
    	   	   a string with $pec1al char's, punctuation and escapes \\ \" \t and
    	   	   maybe crabs 🦀\""`

## Сложные(составные) типы данных

 - `array` - обозначается квадратными скобками внутри которых через
   запятую перечислены его элементы. Могут быть пустыми. игнорируют
   пробелы между элементами, то есть их может быть сколь угодно много
   или не быть вообще. Элементами могут быть любые типы данных, простые
   или сложные, массивы, сеты и мапы любой вложенности.  
	- `[1, 2.0, "text", ["another", {"set"}, <"map_key": ["map value"]>], []]` 
   
 - `set` - обозначается фигурными скобками внутри которых через запятую
  перечислены его элементы. Могут быть пустыми. игнорируют пробелы
  между элементами, то есть их может быть сколь угодно много или не
  быть вообще. Элементами могут быть любые типы данных, простые или
  сложные, массивы, сеты и мапы любой вложенности. Элементы могут повторяться, но в вычислениях повторы игнорируются.
	   - см. array, одна фигня, только с такими вот скобками `{}`
   
   
 - `map` - обозначается вот этими штуками `< >`, внутри них перечисляются   
   пары ключ-значение разделенные запятой. Сами пары разделены   
   двоеточием. Могут быть пустыми. игнорируют пробелы как между   
   элементами, так и внутри пар, то есть их может быть сколь угодно   
   много или не быть вообще. И ключами и значениями могут быть любые   
   типы данных, простые или сложные, массивы, сеты и мапы любой   
   вложенности. Элементы могут повторяться, но в вычислениях повторы игнорируются.
   - `<"key": 1, <"map_as_key": 2>: 2.0, {}: {[10, 20]}, "karl": [], true: null>`


## Функции

**Предисловие**

Все функции ожидают строку в первом агрументе. Эта строка описывает как из фильтруемых данных достать нужный элемент для сравнения. 
Пример - имеем json `{"a": {"b": 1}}`. если хотим достать вложенный джейсон `{"b": 1}` по ключу `"a"`, то в функцию пишем `"/a"`, если хотим достать `1`, то пишем `"/a/b"`. Конкретный формат записи не ограничен, то есть не обязательно разделять уровни через `/`. Сам формат определяется либо пользователем языка, либо типом фильтруемых данных. Аргумент просто должен быть валидным `string`. 

**Сами функции**

 - `compare(lhs op rhs)` 
	 - lhs - любой валидный string 
	 - op - операция сравнения, одна из `!=, ==, >, >=, <, <=`
	 - rhs - любой валидный простой или сложный тип. 
		 - Пример - имеем джейсон `{"a": {"b": 1}}`
			  > Значение по пути /a/b должно быть больше 0

			  > `compare("/a/b" > 0)`
			 
	   **Важно!** Умеет работать только с одинаковыми типами данных. То есть
	   если по пути указанному в `lhs` будет `integer`, то `rhs` тоже должен быть
	   `integer`. Иначе - ошибка
   
  - `intersects(lhs rhs)` 
	  - lhs - любой валидный `string`, но по пути должен лежать `string/array/set/map`
	  - rhs - `string/array/set/map`
	 

		> Функция для поиска пересечения между значениями в `lhs` и `rhs`. То
		> есть 	  проверка того, есть ли у значения в `lhs` и `rhs` **хотя бы 1
		> общий 	  элемент**.
			
		- Пример - имеем json `{"a": [1, 2, 3]}`. 
			 > Есть хотя бы один общий элемент между значением по пути "/a" и [1, 5, 6]

			 > `intersects("/a" [1, 5, 6])` 
		
	  **Важно!** Умеет работать только с одинаковыми
	  типами данных. То есть если по пути указанному в `lhs` будет `array`, то
	  `rhs` тоже должен быть `array`. Иначе - ошибка
   
   - `is_contained(lhs rhs)`
	   - lhs - любой валидный `string`
	   - rhs - `array/set/map` 

			> Функция для проверки того, содержит ли `rhs` в себе значение по пути из `lhs`  
			- Пример имеем json `{"a": {"b": 1}}`.
				>  "есть ли среди [1, 2, 3, 4] значение из "/a/b"" 

				>  `is_contained("/a/b" [1, 2, 3, 4])`

   
   - `exists(path)` 
	   - path - любой валидный string. 
		    >Проверка на то, что значение в path существует, но при этом не важно, чем оно является. 
		   - Пример - имеем json `{"a": [1, 2, 3]}`. 
				> Условие "должно существовать какое-то значение по пути "/a"

				>`exists("/a")`
   
   - `is_subset(lhs rhs)` 
	   - lhs - любой валидный `string`. По пути в `lhs` должен быть `string/array/set/map`. 
	   - rhs - `string/array/set/map` 

			> Функция проверяет, что значение по пути в `lhs` полностью содержится в   
			> `rhs`
			- Пример - имеем json `{"a": [1, 2, 3]}`. 
				> Значение по ключу "/a" является частью [1, 2, 3, 4] 

				> `is_subset("/a" [1, 2, 3, 4])`
			
	   **Важно!** Умеет работать только с одинаковыми типами данных. То есть
	   если по пути указанному в `lhs` будет `array`, то `rhs` тоже должен быть
	   `array`. Иначе - ошибка
   
   - `is_superset(lhs rhs)` - тоже самое, что `is_subset`, только наоборот.
	   > Значение `rhs` должно полностью содержаться в `lhs`

## Бинарные условия

- `and(lhs, rhs)`
	-  `lhs` и `rhs` - любая функция/бинарное условие. 
		> Проверяет, что выполняются оба.	
		- Пример - имеем json `{"a": {"b": 1}}`. 
			> Cуществует какое-то значение по пути "/a" и значение по пути "/a/b" больше 0

            > `and(exists("/a"), compare("/a/b" > 0))`

- `or(lhs, rhs)` - тоже самое, что `and`, но проверяет, что выполняется хотя бы одно из двух

- `xor(lhs, rhs)` - тоже самое, что `or`, но проверяет, что выполняется строго одно из двух, но не оба

Бинарные условия можно вкладывать на любую глубину

    and(
    	xor(
    		or(compare, and(exists, compare)),
    		intersects
    		),
    	exists
    ) 

## Отрицание
- `!` - ставится перед любыми функциями или бинарными условиями
	- Пример - имеем json `{"a" : {"b": 1}}`
		> Значение по пути "/c" не существует

		> `!exists("/c")`
